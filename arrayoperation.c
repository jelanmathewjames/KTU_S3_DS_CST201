#include <stdio.h>
#include <stdlib.h>
#define MAX 10

int *arr = NULL;

void initarray(){
	if(arr == NULL)
		arr = malloc(MAX*sizeof (int);
 else
	printf("array already intialised");
}
void insertarray(){
}
void setarray(){
}

int main(){
}
